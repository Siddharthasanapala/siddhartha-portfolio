GEMINI_API_KEY=
CONTACT_EMAIL=siddharthasanapala136@gmail.com
RESEND_API_KEY=
# If using SMTP instead of Resend:
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASS=
# Public site URL used for canonical links, OpenGraph/Twitter metadata, and
# sitemap.xml/robots.txt — set to the real deployed domain in production.
SITE_URL=http://localhost:3000
NODE_ENV=production
