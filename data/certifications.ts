import type { CertificationItem } from "@/types";

/**
 * No certifications were present in the source resume (Build Spec §6.8).
 * Keep this empty until real data is supplied — the Certifications
 * component renders nothing while this array is empty.
 */
export const certifications: CertificationItem[] = [];
